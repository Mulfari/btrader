name: 🚀 Ping Railway Service

on:
  schedule:
    - cron: '*/5 * * * *'    # cada 5 minutos
  workflow_dispatch:         # te permite ejecutarlo manualmente

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: |
          curl -fsS https://btrader-production.up.railway.app/health || echo "❌ Ping fallido" 